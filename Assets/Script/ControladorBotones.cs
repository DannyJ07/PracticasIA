using UnityEngine;
using UnityEngine.UI;

public class ControladorBotones : MonoBehaviour
{
    public Button botonPatron1;
    public Button botonPatron2;
    public Button botonPatron3;
    public MovPatron movPatron;

    void Start()
    {
        /*botonPatron1.onClick.AddListener(() => movPatron.SetPatron(movPatron.patron1));
        botonPatron2.onClick.AddListener(() => movPatron.SetPatron(movPatron.patron2));
        botonPatron3.onClick.AddListener(() => movPatron.SetPatron(movPatron.patron3));*/
    }
}

